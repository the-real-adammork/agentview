# E2E baseline-vs-branch comparison (proves zero behavior change)

The full Playwright suite was run twice on this machine:

1. On the Phase 1 branch (handlers routed through CodexSource).
2. On the baseline (Task 3 handler changes stashed, i.e. handlers still call
   openStateStore/getRolloutFactsWithCache/tailRolloutFile directly — commit 5cac5dd).

Both runs produced the IDENTICAL pass/fail set:

  6 failed
    tests/e2e/diagnostics-ui.spec.ts:133  Diagnostics UI @diagnostics  (out of Phase 1 scope; untouched)
    tests/e2e/diagnostics-ui.spec.ts:195  Diagnostics UI @diagnostics  (out of Phase 1 scope; untouched)
    tests/e2e/feed-enter-animation.spec.ts:22  feed-enter live insert animation  (out of Phase 1 scope; untouched)
    tests/e2e/graph-tokens.spec.ts:12  @graph-tokens
    tests/e2e/timeline-detail.spec.ts:13  @timeline (renders parsed rollout rows ...)
    tests/e2e/timeline-detail.spec.ts:92  @timeline (surfaces enriched observed rollout facts ...)
  15 passed

Tagged-only run (@sessions|@timeline|@graph-tokens) on both branch and baseline:
  3 failed (graph-tokens:12, timeline-detail:13, timeline-detail:92)
  7 passed

Conclusion: the 6 (full) / 3 (tagged) e2e failures are PRE-EXISTING in this local
environment and are NOT introduced by the Phase 1 refactor. They are UI-visibility
timeouts (e.g. getByLabel('Timeline events').getByText('Timeline task started') not
visible within 5s) that reproduce identically with the refactor reverted.

API-level byte-identity is independently proven by:
  - tests/integration/timelineApi.test.ts, sessionsApi.test.ts, graphTokensApi.test.ts,
    sourceErrors.test.ts (all green, unmodified) — the API envelopes, status codes,
    cacheStatus, warnings, and subtree ordering are unchanged.
  - direct-api-proof.txt — /api/timeline served through CodexSource returns ok:true
    with the expected events/facts.
