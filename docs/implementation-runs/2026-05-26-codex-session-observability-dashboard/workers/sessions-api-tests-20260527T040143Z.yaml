status: test_proposed
result: fail_expected
worker_id: worker-sessions-api-tests
run_id: 2026-05-26-codex-session-observability-dashboard
phase: "Phase 2 - Sessions Index"
task: "Task 2: Wire Health, Sessions, And Thread API Routes"
worker_branch: impl/phase-2-sessions-api-tests
worker_worktree: /Users/adam/Projects/agentview/.worktrees/impl-phase-2-sessions-api-tests
base_commit: 1686649ac5d6f6fc56113e4a41e3192384f341d6
changed_test_files:
  - tests/integration/sessionsApi.test.ts
changed_artifacts:
  - docs/qa/artifacts/2026-05-26-codex-session-observability-dashboard-phase-2/task-2-test-proposal.txt
command:
  run: "npm run test -- --run tests/integration/sessionsApi.test.ts"
  result: fail_expected
  exit_code: 1
  artifact: docs/qa/artifacts/2026-05-26-codex-session-observability-dashboard-phase-2/task-2-test-proposal.txt
expected_failure:
  - "7 of 8 route tests fail because src/backend/server.ts still serves fixture API envelopes for /api/health and /api/sessions instead of state-db routes."
  - "GET /api/sessions/:threadId returns fixture not_found instead of a state-db getThread lookup."
  - "Unsupported schema, missing state_5.sqlite, and invalid filters currently return fixture success responses instead of typed state-db error envelopes."
requirement_mapping:
  health_source_availability: "GET /api/health must return ApiResult source state-db, mode real, checkedAt, and stateDb schema/readOnly/table availability from the Task 1 state store."
  sessions_first_paint: "GET /api/sessions must read real temp SQLite fixture rows, sort by updated_at_ms desc, include child/open counts and async badge statuses, exclude archived by default, and avoid rollout JSONL reads under a runtime fs trap."
  get_thread_lookup: "GET /api/sessions/:threadId must return one normalized SessionSummary for an existing thread and THREAD_NOT_FOUND with 404 for unknown ids."
  unsupported_schema: "Malformed state_5.sqlite fixtures must produce SCHEMA_UNSUPPORTED with 503 and missing schema detail instead of falling back to fixtures."
  missing_db: "A CODEX_HOME without state_5.sqlite must produce STATE_DB_MISSING with 503 instead of falling back to fixtures."
  invalid_filters: "archived, source, minTokens, limit, and offset validation must reject invalid values with INVALID_FILTER before querying rows."
  local_only_behavior: "Loopback origins must receive access-control-allow-origin while non-loopback origins do not."
service_wiring_rows:
  - row: "Health and source availability"
    evidence: "health route asserts state-db source, real mode, schema tables/readOnly; missing DB and unsupported schema assert typed source-unavailable envelopes."
  - row: "Sessions first paint"
    evidence: "list route asserts temp SQLite rows, updated_at_ms order, child/open counts, archived exclusion, and no .jsonl filesystem reads."
  - row: "Session lookup"
    evidence: "thread lookup route asserts /api/sessions/:threadId success and 404 THREAD_NOT_FOUND."
  - row: "Filter composition"
    evidence: "list route asserts composed search/cwd/source/role/model/archive/minTokens/warningStatus/failedToolStatus filters and invalid-filter rejection."
mock_fixture_disclosure:
  automated_tests: "real local HTTP server process started with npm run api and per-test AGENTVIEW_API_PORT"
  data_fixture: "real temp CODEX_HOME/state_5.sqlite generated by tests/fixtures/codexHome.ts from Task 1"
  jsonl_read_guard: "runtime NODE_OPTIONS preloader patches node:fs read/open/readStream calls and fails/logs if a .jsonl path is read during sessions first paint"
  mocks: "no mocked route handlers; tests use fetch against the local backend server"
  mock_only_risk: "fixture schema may not cover private Codex state_5.sqlite drift; fs/promises imports from node:fs/promises may not all be intercepted by the node:fs preloader guard"
  required_real_dependencies: "Node 24 node:sqlite, npm api script, local loopback HTTP, temp filesystem"
blockers:
  - "Task 2 production files are intentionally not implemented yet: src/backend/api/health.ts, src/backend/api/sessions.ts, state-db route registration in src/backend/server.ts, and client contract changes."
  - "Task 1 state store currently only exposes listSessions; Task 2 implementation needs getThread and extended filters/source validation to satisfy these tests."
worker_result_yaml_path: /Users/adam/Projects/agentview/.worktrees/impl-phase-2-sessions-api-tests/docs/implementation-runs/2026-05-26-codex-session-observability-dashboard/workers/sessions-api-tests-20260527T040143Z.yaml
commit: null
